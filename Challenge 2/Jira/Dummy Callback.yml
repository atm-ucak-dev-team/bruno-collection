info:
  name: Dummy Callback
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/auth/jira/dummy-callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
