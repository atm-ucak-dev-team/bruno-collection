info:
  name: Connect email
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/v1/email/credentials"
  headers:
    - name: X-User-Dummy-Id
      value: test-user-123
  body:
    type: json
    data: |-
      {
        "email_address" : "dfbomanarakasura26@cjkt.idserve.net",
        "password" : "iVn&1jl%yl"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
